# shows requests tied up with  a user by user_id
requests = {
  1:{
    'title':'req1', 
    'user_id': 1
  },
  2:{
    'title': 'req2',
    'user_id':1
  },
  3:{
    'title': 'req3',
    'user_id': 2
  },
  4:{
    'title': 'req4',
    'user_id': 1
  }
}

# for request in requests.values():
#   print(request)

user_id = 2
user_reqs = []
for user_req in requests.values():
  if user_req.get('user_id') == user_id:
    user_reqs.append(user_req)
    print(user_reqs)

